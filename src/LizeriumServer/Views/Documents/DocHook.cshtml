@using LizeriumServer.FormatsData.AppSeo
@inject IViewLocalizer Localizer
@section PageStyle {
    <link rel="stylesheet" href="~/css/doc_hook.min.css" asp-append-version="true" />
}
@model DocumentViewModel
@{
    var nameCategoryLocalized = Model.CategoriesHook.FirstOrDefault(it =>
        it.Key == Model.Category);
    string categoryName = Model.Category;
    if (nameCategoryLocalized != null)
    {
        categoryName = CultureInfo.CurrentUICulture.TwoLetterISOLanguageName switch
        {
            "en" => nameCategoryLocalized?.NameEn ?? Model.Category,
            _ => nameCategoryLocalized?.NameRu ?? Model.Category
        };
    }
    ViewData["Title"] = Localizer["DocHookItem_Title_Site"].Value + categoryName;
}

<section class="doc-hook-page doc-hook-detail">
    <header class="doc-hook-hero doc-hook-detail-hero lizerium-reveal">
        <span class="doc-hook-hero-icon" aria-hidden="true">
            <img src="~/img/pages/home/ecosystem-server.webp" alt="" />
        </span>
        <div class="doc-hook-hero-copy">
            <span>@SharedLocalizer["Shared_Commands_List"]</span>
            <h1 id="@Model.Category">@categoryName</h1>
            <p>@Localizer["DocHookItem_Description"]</p>
        </div>
    </header>

    <article class="markdown-body doc-hook-shell lizerium-reveal">
        <a class="doc-hook-back custom-cursor-hover" href="/docs/hook">
            <span class="doc-hook-back-icon" aria-hidden="true">↵</span>
            <span>@SharedLocalizer["Shared_Commands_List"]</span>
        </a>

    <div class="search_doc_hook doc-hook-search custom-cursor-hover mb-1 mt-1 lizerium-reveal">
        <input type="text" id="commandSearch" class="form-control mb-3" data-category="@Model.Category" placeholder="@Localizer["DocHookItem_Search_Placeholder"]">
        <div id="searchResults" class="doc-hook-search-results"></div>
    </div>

    <!-- Пагинация только для этой категории -->
    <nav class="doc-hook-pagination lizerium-reveal">
        @if (Model.CommandsCount.TryGetValue(Model.Category, out var count))
        {
            int pages = (int)Math.Ceiling(count / (double)Model.PageSize);
            if (pages > 1)
            {
                <ul class="pagination d-flex align-items-end m-0 flex-row p-0">
                    @for (int i = 1; i <= pages; i++)
                    {
                        if (i == Model.Page)
                        {
                            <li class="page-item active"><a class="page-link" href="#">@i</a></li>
                        }
                        else
                        {
                            <li class="page-item"><a class="page-link" href="/docs/hook/@Model.Category?page=@i">@i</a></li>
                        }
                    }
                </ul>
            }
        }
    </nav>
    @foreach (var command in Model.Commands)
    {
        var currentLang = CultureInfo.CurrentUICulture.TwoLetterISOLanguageName;

        // Проверяем, есть ли перевод для текущей локали
        string displayName = command.Description;

        if (command.Translations != null
                && command.Translations.TryGetValue(currentLang, out var translationsList)
                && translationsList.Any())
        {
            displayName = translationsList.First();
        }
        else
        {
            displayName = command.Description ?? "No description";
        }

        var statusClass = command.Status switch
        {
            1 => "is-active",
            2 => "is-disabled",
            3 => "is-develop",
            _ => "is-neutral"
        };

        var statusText = command.Status switch
        {
            1 => Localizer["DocHookItem_Command_State3"].Value,
            2 => Localizer["DocHookItem_Command_State1"].Value,
            3 => Localizer["DocHookItem_Command_State2"].Value,
            _ => string.Empty
        };

        var hasGif = !string.IsNullOrEmpty(command.UrlGif);

        <section class="command_block doc-hook-command-card lizerium-reveal">
            <div class="doc-hook-command-head">
                <div class="doc-hook-command-aliases">
                    @foreach (var name in command.CommandNamesList)
                    {
                        <div class="command_div" id="hook_@command.ClearId(@name)">
                            <code class="language-bash name_c">@name</code>
                        </div>
                    }
                </div>
            </div>

            <div class="doc-hook-command-description">
                @displayName
            </div>

            @if (hasGif)
            {
                <figure class="doc-hook-command-gif">
                    <a class="doc-hook-command-gif-link" href="/gifs/@command.UrlGif" target="_blank" rel="noopener">
                        <img src="/gifs/@command.UrlGif" alt="">
                    </a>
                </figure>
            }

            <div class="doc-hook-command-footer">
                @if (!string.IsNullOrWhiteSpace(statusText))
                {
                    <span class="doc-hook-status @statusClass">@statusText</span>
                }
            </div>
        </section>
    }
    <!-- Пагинация только для этой категории -->
    <nav class="doc-hook-pagination lizerium-reveal">
        @if (Model.CommandsCount.TryGetValue(Model.Category, out var countLow))
        {
            int pages = (int)Math.Ceiling(countLow / (double)Model.PageSize);
            if (pages > 1)
            {
                <ul class="pagination d-flex align-items-end m-0 flex-row p-0">
                    @for (int i = 1; i <= pages; i++)
                    {
                        if (i == Model.Page)
                        {
                            <li class="page-item active"><a class="page-link" href="#">@i</a></li>
                        }
                        else
                        {
                            <li class="page-item"><a class="page-link" href="/docs/hook/@Model.Category?page=@i">@i</a></li>
                        }
                    }
                </ul>
            }
        }
    </nav>

    <button class="button-copy-code hidden"></button>
    <svg aria-hidden="true" height="16" viewBox="0 0 16 16" version="1.1" width="16" data-view-component="true"
         class="copy-docs-icon hidden">
        <path fill-rule="evenodd"
              d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z">
        </path>
        <path fill-rule="evenodd"
              d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z">
        </path>
    </svg>
    <div class="doc-hook-gif-lightbox" hidden aria-hidden="true">
        <button class="doc-hook-gif-lightbox-close" type="button" aria-label="Close">×</button>
        <img src="" alt="">
    </div>
    <script>
        const copyIcon = `<svg aria-hidden="true" height="16" viewBox="0 0 16 16" version="1.1" width="16" data-view-component="true" fill="#f1f1f1" class="copy-docs-icon">
                      <path fill-rule="evenodd" d="M0 6.75C0 5.784.784 5 1.75 5h1.5a.75.75 0 010 1.5h-1.5a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-1.5a.75.75 0 011.5 0v1.5A1.75 1.75 0 019.25 16h-7.5A1.75 1.75 0 010 14.25v-7.5z"></path>
                      <path fill-rule="evenodd" d="M5 1.75C5 .784 5.784 0 6.75 0h7.5C15.216 0 16 .784 16 1.75v7.5A1.75 1.75 0 0114.25 11h-7.5A1.75 1.75 0 015 9.25v-7.5zm1.75-.25a.25.25 0 00-.25.25v7.5c0 .138.112.25.25.25h7.5a.25.25 0 00.25-.25v-7.5a.25.25 0 00-.25-.25h-7.5z"></path>
                    </svg>`;

        const copiedIcon = `<svg aria-hidden="true" height="16" viewBox="0 0 16 16" version="1.1" width="16" data-view-component="true" fill="#f1f1f1" class="docs-copied-icon">
                      <path fill-rule="evenodd" d="M13.78 4.22a.75.75 0 010 1.06l-7.25 7.25a.75.75 0 01-1.06 0L2.22 9.28a.75.75 0 011.06-1.06L6 10.94l6.72-6.72a.75.75 0 011.06 0z"></path>
                    </svg>`;

        const blocks = document.querySelectorAll("div.command_div");

        blocks.forEach((block) => {
            if (!navigator.clipboard) {
                return;
            }

            let button = document.createElement("button");
            button.className = "button-copy-code";
            button.style.visibility = "visible";
            button.style.backgroundColor = "#383838";
            button.innerHTML = copyIcon;
            block.appendChild(button);

            button.addEventListener("click", async () => {
                await copyCode(block);
            });
        });

        async function copyCode(block) {
            let copiedCode = block.cloneNode(true);
            copiedCode.removeChild(copiedCode.querySelector("button.button-copy-code"));

            const html = copiedCode.outerHTML.replace(/<[^>]*>?/gm, "");

            block.querySelector("button.button-copy-code").innerHTML = copiedIcon;
            setTimeout(function () {
                block.querySelector("button.button-copy-code").innerHTML = copyIcon;
            }, 2000);

            const parsedHTML = htmlDecode(html);

            await navigator.clipboard.writeText(parsedHTML);
        }

        function htmlDecode(input) {
            const doc = new DOMParser().parseFromString(input, "text/html");
            return doc.documentElement.textContent;
        }

        const gifLightbox = document.querySelector(".doc-hook-gif-lightbox");
        const gifLightboxImage = gifLightbox?.querySelector("img");
        const gifLightboxClose = gifLightbox?.querySelector(".doc-hook-gif-lightbox-close");

        document.querySelectorAll(".doc-hook-command-gif-link").forEach((link) => {
            link.addEventListener("click", (event) => {
                if (!gifLightbox || !gifLightboxImage) {
                    return;
                }

                event.preventDefault();
                gifLightboxImage.src = link.href;
                gifLightbox.hidden = false;
                gifLightbox.setAttribute("aria-hidden", "false");
                document.documentElement.classList.add("doc-hook-lightbox-open");
            });
        });

        function closeGifLightbox() {
            if (!gifLightbox || !gifLightboxImage) {
                return;
            }

            gifLightbox.hidden = true;
            gifLightbox.setAttribute("aria-hidden", "true");
            gifLightboxImage.src = "";
            document.documentElement.classList.remove("doc-hook-lightbox-open");
        }

        gifLightboxClose?.addEventListener("click", closeGifLightbox);
        gifLightbox?.addEventListener("click", (event) => {
            if (event.target === gifLightbox) {
                closeGifLightbox();
            }
        });
        document.addEventListener("keydown", (event) => {
            if (event.key === "Escape") {
                closeGifLightbox();
            }
        });
    </script>
</article>
</section>
